input AuthInput {
  email: String!
  password: String!
}

type AuthOutput {
  user: User
  access_token: String!
}

type Query {
  login(loginInput: AuthInput): AuthOutput
}

type Mutation {
  signup(signupInput: AuthInput): AuthOutput
}
